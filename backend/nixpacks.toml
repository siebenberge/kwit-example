[phases.setup]
nixpkgsArchive = "master"
aptPkgs = ["unzip"] # Required for Bun installation scripts

[phases.install]
cmds = ["curl -fsSL https://bun.sh/install | bash", "bun install"]
paths = ["/root/.bun/bin"] # Adds Bun to the system PATH
